version: "3.9"

services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./app_data.db:/app/app_data.db
      - ./logs:/app/logs
      - ./reports:/app/reports
      - ./chroma_db:/app/chroma_db
    restart: unless-stopped